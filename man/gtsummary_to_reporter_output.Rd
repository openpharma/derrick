% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtsummary_to_reporter_output.r
\name{gtsummary_to_reporter_output}
\alias{gtsummary_to_reporter_output}
\title{Export \code{gtsummary} tables to clinical-style \code{reporter} outputs}
\usage{
gtsummary_to_reporter_output(
  gts_obj,
  file_path = "Clinical_Report.rtf",
  max_table_width = NULL,
  min_col_width = 0.6,
  column_widths = NULL,
  column_labels = NULL,
  spanning_headers = NULL,
  report_orientation = "landscape",
  report_paper_size = "letter",
  report_units = "inches",
  report_margins = NULL,
  report_font_size = 9,
  indent_unit = 1,
  output_types = c("RTF", "TXT"),
  save_rds = TRUE,
  rds_dir = "rds",
  rows_per_page = NULL,
  debug_indent = FALSE,
  debug_spanning = FALSE,
  group_columns = NULL,
  group_blank_after = TRUE
)
}
\arguments{
\item{gts_obj}{A \code{gtsummary} object (with \code{table_body}/\code{table_styling}) or
a plain \code{data.frame} to export.}

\item{file_path}{Output path. The extension is ignored and output files are
written according to \code{output_types}.}

\item{max_table_width}{Maximum total table width. If \code{NULL}, derived from
\code{report_paper_size}, \code{report_orientation}, \code{report_units}, and
\code{report_margins}.}

\item{min_col_width}{Minimum width allowed for any column.}

\item{column_widths}{Optional manual column widths, either numeric vector or
a \code{"|"}-delimited string (e.g. \code{"10|5|5"}).}

\item{column_labels}{Optional column header overrides, as a named vector/list
or a data frame with \code{column} and \code{label}.}

\item{spanning_headers}{Optional spanning header definitions, as a data frame
or list with fields \code{from}, \code{to}, and \code{label}.}

\item{report_orientation}{Page orientation (\code{"landscape"} or \code{"portrait"}).}

\item{report_paper_size}{Paper size (\code{"letter"}, \code{"legal"}, \code{"a4"}, \code{"rd4"},
\code{"none"}, or numeric length-2 vector).}

\item{report_units}{Units for dimensions (\code{"inches"} or \code{"cm"}).}

\item{report_margins}{Optional margins as named vector/list (\code{top}, \code{right},
\code{bottom}, \code{left}) or numeric length-4 vector.}

\item{report_font_size}{Base report font size.}

\item{indent_unit}{Number of spaces per indent level in \code{label}.}

\item{output_types}{Output types to write; supported values are \code{"RTF"} and
\code{"TXT"}.}

\item{save_rds}{Logical; when \code{TRUE}, save processed output data and ARD
object (if available) as \code{.rds} files.}

\item{rds_dir}{Reserved argument for backward compatibility.}

\item{rows_per_page}{Optional maximum number of table rows per page. If
\code{NULL}, estimated from page geometry and \code{report_font_size}.}

\item{debug_indent}{Logical; print indentation diagnostics.}

\item{debug_spanning}{Logical; print spanning-header diagnostics.}

\item{group_columns}{Optional grouping columns to hide and apply
\code{blank_after}.}

\item{group_blank_after}{Logical; whether to apply \code{blank_after} based on
grouping columns.}
}
\value{
A character vector containing generated output file paths.
}
\description{
Convert a \code{gtsummary} object (or a plain \code{data.frame}) into a \code{reporter}
table and write RTF and/or TXT outputs. The function is designed for
clinical reporting workflows and supports column labels, spanning headers,
pagination, indentation handling, and optional export of intermediate data.

Environment variables are automatically consumed when available:
\code{title1}-\code{title9}, \code{footnote1}-\code{footnote9}, and \code{progname}.
}
\examples{
\dontshow{if (requireNamespace("reporter", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
gts_tbl <- gtsummary::trial |>
  dplyr::select(trt, age, grade) |>
  gtsummary::tbl_summary(by = trt) |>
  gtsummary::add_p()

out <- gtsummary_to_reporter_output(
  gts_obj = gts_tbl,
  file_path = tempfile("clinical_report_", fileext = ".rtf"),
  output_types = "TXT",
  save_rds = FALSE
)

out
\dontshow{\}) # examplesIf}
}
