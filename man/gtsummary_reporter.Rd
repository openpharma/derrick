% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtsummary_reporter.R
\name{gtsummary_reporter}
\alias{gtsummary_reporter}
\title{Export \code{gtsummary} tables to clinical-style \code{reporter} outputs}
\usage{
gtsummary_reporter(
  gts_obj,
  file_path = "Clinical_Report.rtf",
  max_table_width = NULL,
  min_col_width = 0.6,
  column_widths = NULL,
  column_labels = NULL,
  spanning_headers = NULL,
  report_orientation = "landscape",
  report_paper_size = "letter",
  report_units = "inches",
  report_margins = NULL,
  report_font_size = 9,
  indent_unit = 1,
  output_types = c("RTF", "TXT"),
  save_rds = TRUE,
  rds_dir = "rds",
  rows_per_page = NULL,
  max_chars_per_line = NULL,
  debug_indent = FALSE,
  debug_spanning = FALSE,
  group_columns = NULL,
  group_blank_after = TRUE
)
}
\arguments{
\item{gts_obj}{A \code{gtsummary} object (with \code{table_body}/\code{table_styling}) or
a plain \code{data.frame} to export.}

\item{file_path}{Output path. The extension is ignored and output files are
written according to \code{output_types}.}

\item{max_table_width}{Optional maximum total table width in \code{report_units}.
If \code{NULL}, reporter chooses the automatic table width. Non-NULL values are
capped at \code{page_width - left_margin - right_margin}. With the defaults
(\code{report_paper_size = "letter"},
\code{report_orientation = "landscape"}, \code{report_units = "inches"}, and
default 1-inch left/right margins), the maximum effective value is \code{9}.
For common defaults in inches: letter landscape = \code{9}, letter portrait =
\code{6.5}, A4 landscape = \code{9.69}, A4 portrait = \code{6.27}, RD4 landscape =
\code{8.70}, RD4 portrait = \code{5.70}, legal landscape = \code{12}, legal portrait =
\code{6.5}. The same defaults in centimeters are: letter landscape = \code{22.86},
letter portrait = \code{16.51}, A4 landscape = \code{24.62}, A4 portrait = \code{15.92},
RD4 landscape = \code{22.22}, RD4 portrait = \code{14.52}, legal landscape =
\code{30.48}, legal portrait = \code{16.51}. \code{report_paper_size = "none"} gives an
infinite page width. If margins exceed the physical page width, the
effective width is \code{0}. Use \code{NULL} unless you intentionally want to
constrain the table.}

\item{min_col_width}{Minimum width allowed for manually supplied
\code{column_widths}, in \code{report_units}.}

\item{column_widths}{Optional manual column widths in \code{report_units}, either
a numeric vector or a \code{"|"}-delimited string. Values are applied in display
column order, usually the \code{label} column first, followed by statistic or
data columns. For example, on the default 9-inch usable width, a 4-column
table could use \code{"3|2|2|2"}; a 5-column table could use \code{"3|1.5|1.5|1.5|1.5"}.
The practical total range is from \code{n_cols * min_col_width} up to the
effective \code{max_table_width}; if the supplied total is larger, widths are
scaled down to fit while respecting \code{min_col_width} where possible. If
\code{n_cols * min_col_width} is wider than the effective page width, the
per-column floor is relaxed to \code{effective_width / n_cols}. If fewer widths
than columns are supplied, the last width is repeated; if more are supplied,
extras are ignored. Use \code{NULL} unless you need precise control over column
allocation. When \code{NULL}, reporter calculates column widths using its
output-specific layout metrics.}

\item{column_labels}{Optional column header overrides, as a named vector/list
or a data frame with \code{column} and \code{label}.}

\item{spanning_headers}{Optional spanning header definitions, as a data frame
or list with fields \code{from}, \code{to}, and \code{label}.}

\item{report_orientation}{Page orientation (\code{"landscape"} or \code{"portrait"}).}

\item{report_paper_size}{Paper size (\code{"letter"}, \code{"legal"}, \code{"A4"}, \code{"RD4"},
\code{"none"}, or numeric length-2 vector).}

\item{report_units}{Units for dimensions (\code{"inches"} or \code{"cm"}).}

\item{report_margins}{Optional margins as named vector/list (\code{top}, \code{right},
\code{bottom}, \code{left}) or numeric length-4 vector.}

\item{report_font_size}{Base report font size.}

\item{indent_unit}{Number of spaces per indent level in \code{label}.}

\item{output_types}{Output types to write; supported values are \code{"RTF"},
\code{"TXT"}, \code{"DOCX"}, \code{"PDF"}, and \code{"HTML"}.}

\item{save_rds}{Logical; when \code{TRUE}, save processed output data and ARD
object (if available) as \code{.rds} files.}

\item{rds_dir}{Reserved argument for backward compatibility.}

\item{rows_per_page}{Optional maximum number of table rows per manual chunk.
If \code{NULL}, no manual pre-splitting is done; reporter handles pagination in
\code{write_report()} using its output-specific layout algorithm.}

\item{max_chars_per_line}{Optional integer. For TXT output, constrains the
total table width so that at most this many characters fit across one TXT
line (Courier at 12 CPI). Applied after \code{max_table_width}, so the stricter
limit wins for TXT. Other output formats are not constrained by this
argument. Title and footnote wrapping is delegated to reporter.}

\item{debug_indent}{Logical; print indentation diagnostics.}

\item{debug_spanning}{Logical; print spanning-header diagnostics.}

\item{group_columns}{Optional grouping columns to hide and apply
\code{blank_after}.}

\item{group_blank_after}{Logical; whether to apply \code{blank_after} based on
grouping columns.}
}
\value{
A character vector containing generated output file paths.
}
\description{
Convert a \code{gtsummary} object (or a plain \code{data.frame}) into a \code{reporter}
table and write RTF, TXT, DOCX, PDF, and/or HTML outputs. The function is
designed for clinical reporting workflows and supports column labels,
spanning headers, pagination, indentation handling, and optional export of
intermediate data.
}
\details{
Environment variables are automatically consumed when available:
\code{title1}-\code{title9}, \code{footnote1}-\code{footnote9}, and \code{progname}.

Widths are resolved in this order:
\enumerate{
\item Compute the usable page width as
\code{page_width - left_margin - right_margin}.
\item If \code{max_table_width} is supplied, cap it at that usable width and pass it
to \code{reporter::create_table()}. If it is \code{NULL}, no table width is passed
and reporter calculates the automatic width for each output format.
\item For TXT output only, apply \code{max_chars_per_line} as an additional
character-budget cap (\code{max_chars_per_line / 12} inches), when supplied.
\item If \code{column_widths} is supplied, pass those manual widths to reporter after
scaling them down to the effective table width when needed. If
\code{column_widths} is \code{NULL}, no column widths are passed and reporter
calculates them from the target format, font, font size, headers, and
table contents.
}

Pagination is delegated to \code{reporter} by default. When \code{rows_per_page} is
\code{NULL}, the table is passed to \code{reporter::write_report()} as one table, and
reporter computes page breaks using output-specific fixed metrics plus the
actual wrapped title, header, row, and footnote line counts. Set
\code{rows_per_page} only when you need to force manual chunks before reporter's
own pagination runs.
}
\examples{
\dontshow{if (requireNamespace("reporter", quietly = TRUE) & requireNamespace("gtsummary", quietly = TRUE)) withAutoprint(\{ # examplesIf}
gts_tbl <- gtsummary::trial |>
  dplyr::select(trt, age, grade) |>
  gtsummary::tbl_summary(by = trt) |>
  gtsummary::add_p()

out <- gtsummary_reporter(
  gts_obj = gts_tbl,
  file_path = tempfile("clinical_report_", fileext = ".rtf"),
  output_types = "TXT",
  save_rds = FALSE
)

out
\dontshow{\}) # examplesIf}
}
